import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.calcularperimetro',
  appName: 'calcularperimetro',
  webDir: 'www'
};

export default config;
